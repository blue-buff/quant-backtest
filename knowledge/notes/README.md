# knowledge/notes/ —— 自由笔记区

- claims.jsonl（knowledge/ 下）= 假说与结论，管线自动回写（auto-claim / link_claims），
  也支持手动 python -m pipeline.kb add 提炼。
- 本目录 = agent 自由笔记，markdown 任意格式，自己维护，管线完全不碰。
  例子：想法草稿、文献笔记、踩坑记录、数据分析随笔。
- 不做三层知识库（claims + notes 两层就够；KB 换 sqlite/向量检索在 won't-do）。
