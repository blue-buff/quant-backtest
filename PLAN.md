# quant-backtest 项目开发计划

> 状态：执行中。本计划基于 2026-08-17 项目现状与数据保真性审查结论制定；
> 2026-08-19 已按 OPTIMIZATION.md 完成执行层修复（见下）。

**目标**：把 A股/港股量化回测学习项目升级为一个「保真、可复现、可持续研究」的因子研究流水线，并完成 GitHub 发布。

**当前状态**：
- 已跑通：backtesting.py → rqalpha 真实规则 → qlib ML 因子选股（Alpha158 + LightGBM/Linear，沪深300/中证500）→ 压力测试
- 已产出：压力测试报告（HTML，`D:\quant_backup\report_stress_test.html`）、干净仓库（git 历史已清理）
- 2026-08-19 执行层修复（OPTIMIZATION.md）：P0-1 月末交易日对齐 / P0-2 plan lineage / P1-3 rank-buffer / P1-4 T+1 / P1-5 指标读 mlruns / P2-1~6 / A3 滑点参与率 / A4 停牌重试 / A2 历史成分脚本（baostock 历史参数已验证，2023-06 与当前成分差 74 只）/ C1 pool 注册表 / C2 tests+CI / C4 双配置合并 / B4 bootstrap 检验
- 未完成：GitHub push（缺用户账号信息）；保真版 v2 报告（历史成分 + 滑点重跑后）；B1/B3/B6/B7 研究深化

**技术栈**：Python 3.12 / qlib 0.9.7 / rqalpha 6.3.0 / LightGBM / baostock / backtesting.py / Docker 容器（hermes-1679f5b2）

---

## 阶段 0：GitHub 发布收尾（预计 15 分钟）

| # | 任务 | 文件/命令 | 验证 |
|---|---|---|---|
| 0.1 | 拿到用户 GitHub 用户名/仓库名/可见性 | — | — |
| 0.2 | 配置 git 身份与 remote（user.email 用 GitHub noreply 邮箱保护隐私） | 容器内 git config | `git remote -v` 显示目标 |
| 0.3 | 创建仓库并 push（gh CLI 或 GitHub API；无 gh 则引导用户网页创建） | — | GitHub 页面可见 27 文件 |
| 0.4 | 填写仓库描述 + topics（quantitative-finance/backtesting/a-share/qlib/rqalpha） | GitHub Web UI | 搜索可见 |
| 0.5 | 打 tag `v0.1-backtest` | `git tag` + push | release 页可见 |

**风险**：GitHub 认证可能因容器无 gh/无 token 而卡住 → 备选：用户网页创建空仓库后我们仅 push。

---

## 阶段 1：数据保真升级（预计 2-3 小时，核心价值最大）

### 1.1 历史成分股（消除幸存者偏差）★最高优先级
- **任务**：改写导出脚本，按每半年调仓日拉取**当时的**指数成分，而非当前快照
- **文件**：新建 `qlib_scripts/export_csv_hist.py`；修改 `dump_bin` 调用方式（股票池文件按调仓期分区）
- **开放问题（需先验证）**：baostock `query_hs300_stocks(date=...)` / `query_zz500_stocks(date=...)` 是否支持历史日期参数——先用 3 分钟小实验验证，不支持则调研备选（akshare 历史成分/聚宽因子库/手工维护成分变更表）
- **验证**：① 2023 年成分表与当年公告对比抽查 ② 找一只 2024 年被剔除的股票，确认其在当年池内、剔除后不在 ③ 重跑训练，对比新旧回测差异（预期：收益下调）

### 1.2 滑点与冲击成本模型
- **任务**：在 rqalpha 桥接层加滑点：按日成交量分档（如参与度 5%/10%），小盘股冲击更大
- **文件**：修改 `qlib_examples/rq_strategy_qlib*.py`（调仓时订单价加滑点项）或 rqalpha mod 配置
- **验证**：沪深300 与中证500 各跑一版带滑点回测，量化「滑点吃掉多少超额」；中证500 预期更惨
- **风险**：A股滑点参数无公开权威数据，只能假设分档 → 计划内明确参数假设并做敏感性分析（±50%）

### 1.3 停牌处理
- **任务**：调仓日遇到停牌股：真实规则下无法买入 → 跳过并顺延到下一档候选；持仓股停牌无法卖出 → 继续持有并标记
- **文件**：修改 `rq_strategy_qlib*.py`（策略逻辑层），rqalpha 本身不支持停牌撮合
- **验证**：回测交易记录中无停牌日成交；对比处理前后差异

### 1.4 数据交叉校验脚本（防数据源静默出错）
- **任务**：新建日级抽样校验：baostock vs 腾讯接口 vs akshare，价格差 >0.5% 报警
- **文件**：新建 `qlib_scripts/validate_data.py`
- **验证**：对 10 只股票 × 3 个随机日期跑通，输出差异报告

### 1.5 复权口径统一
- **任务**：决策并文档化复权基准（建议：训练/选股用后复权+因子，收益计算用后复权，避免前复权基准漂移）
- **文件**：文档 + 导出脚本 `adjustflag` 参数调整
- **验证**：重跑后与 1.1 合并出「保真版 v1」数字，与现有报告做差异对比表

### 阶段 1 验收
- 产出《保真版压力测试报告 v2》（HTML），旧报告标注为「含幸存者偏差的历史版本」
- 结论更新进 README

---

## 阶段 2：策略研究深化（每项独立，可插队）

| # | 任务 | 说明 | 验证 |
|---|---|---|---|
| 2.1 | Walk-forward 滚动训练 | 每 6 个月滚动重训，检验超额是否来自单次训练运气 | 各滚动段超额稳定性 |
| 2.2 | 因子库扩展 | Alpha360 / 自定义量价因子 | IC 对比表 |
| 2.3 | 模型扩展 | XGBoost、MLP、模型平均 | 与 LightGBM/Linear 同口径对比 |
| 2.4 | 股票池扩展 | 中证800、创业板指 | 超额是否跨池（已有警示：中证500 失败） |
| 2.5 | 敏感性分析 | 调仓频率（月/双周）、持仓数（20/50/100） | 参数-超额曲线 |

**约定**：每个实验必须进 mlruns（记录参数），结论同步更新报告与 README，防止「跑完就忘」。

---

## 阶段 3：工程化（预计 4-6 小时）

| # | 任务 | 文件 | 验证 |
|---|---|---|---|
| 3.1 | 配置参数化：消除 `/root/quant`、`/root/.qlib` 硬编码 → 环境变量/单配置文件 | 全部脚本 | 换目录克隆后不改代码可跑 |
| 3.2 | 一键流水线：数据→dump→训练→计划→真实规则回测→报告 单命令串联 | 新建 `pipeline.py` 或 Makefile | 干净环境从头跑到报告生成 |
| 3.3 | 单元测试：计划生成、代码格式转换（SH600000↔600519.XSHG）、滑点计算 | `tests/` 目录 | pytest 通过 |
| 3.4 | GitHub Actions：push 时跑数据校验 + 小规模冒烟回测（跳过全量训练） | `.github/workflows/` | CI 绿灯 |
| 3.5 | 结果管理规范：每次实验的 yaml/日志/关键数字归档表 | 文档 | 新实验 30 秒内可查到历史结果 |

---

## 阶段 4：实盘准备（远期，暂不排期）

- 4.1 模拟盘验证（rqalpha `run_type=p` 或券商模拟接口）
- 4.2 风控规则设计（单股上限、行业上限、总仓位、止损）
- 4.3 信号监控与日志告警
- 4.4 诚实决策点：**若保真版（阶段 1）超额大幅缩水 → 可能结论是「不值得实盘」，这也是有价值的结论**

---

## 风险与开放问题

1. **历史成分数据可得性**（1.1）——baostock 能力未验证，是全计划最大不确定性
2. **滑点参数无权威来源**（1.2）——只能假设+敏感性分析
3. **时间投入 vs 收益**——阶段 1 做完后重新评估 2/3 阶段优先级
4. **容器数据安全**——容器无卷挂载，每阶段产物及时 docker cp 到 `D:\quant_backup\`
5. **GitHub 发布是否等待保真版**——可先发布 v0.1（现状+风险声明），保真版作为 v0.2；也可一步到位。**待用户决策**

## 建议执行顺序

**阶段 0（发布）→ 1.1（幸存者偏差）→ 1.2/1.3（成本与停牌）→ 1.4/1.5（校验与口径）→ 出 v2 报告 → 重新评估 → 阶段 2 选 1-2 项 → 阶段 3 按需**
